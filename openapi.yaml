openapi: 3.0.3
info:
  title: Product Service API
  description: REST API for the AWS shop application — provides product catalog data backed by DynamoDB.
  version: 2.0.0

servers:
  - url: https://mv9r6b0fml.execute-api.us-east-1.amazonaws.com/prod
    description: Production

paths:
  /products:
    get:
      summary: Get all products
      description: Returns the full list of products joined with stock counts.
      operationId: getProductsList
      responses:
        '200':
          description: A list of products
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Product'
        '500':
          $ref: '#/components/responses/InternalError'
    post:
      summary: Create a new product
      description: Creates a new product and its stock entry transactionally.
      operationId: createProduct
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateProductInput'
            example:
              title: New Gadget
              description: A shiny new gadget
              price: 199
              count: 10
      responses:
        '201':
          description: Product created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Product'
        '400':
          description: Invalid request payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          $ref: '#/components/responses/InternalError'

  /products/{productId}:
    get:
      summary: Get product by ID
      description: Returns a single product joined with its stock count, or 404 if not found.
      operationId: getProductsById
      parameters:
        - name: productId
          in: path
          required: true
          description: Unique product identifier (UUID)
          schema:
            type: string
      responses:
        '200':
          description: Product found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Product'
        '400':
          description: Missing productId
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Product not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          $ref: '#/components/responses/InternalError'

components:
  responses:
    InternalError:
      description: Internal server error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

  schemas:
    Product:
      type: object
      required:
        - id
        - title
        - price
        - count
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
        description:
          type: string
        price:
          type: number
        count:
          type: integer
          description: Quantity available in stock
    CreateProductInput:
      type: object
      required:
        - title
        - price
      properties:
        title:
          type: string
        description:
          type: string
        price:
          type: number
          minimum: 0
        count:
          type: integer
          minimum: 0
    Error:
      type: object
      required:
        - message
      properties:
        message:
          type: string
